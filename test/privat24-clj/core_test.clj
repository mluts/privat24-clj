(ns privat24-clj.core-test
  (:require [clojure.test :refer :all]
            [privat24-clj.core :refer :all])
  (:use  clj-http.fake))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
