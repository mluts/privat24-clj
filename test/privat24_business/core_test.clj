(ns privat24-business.core-test
  (:require [clojure.test :refer :all]
            [privat24-business.core :refer :all])
  (:use  clj-http.fake))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
